#pragma once
#include <shell/defines/opaque_handle.h>
#include <shell/grammar/grammar_string.h>

synapse_shell_opaque_handle_declare
	(synapse_shell_extension);

synapse_shell_opaque_handle_declare
	(synapse_shell_extension_probe);